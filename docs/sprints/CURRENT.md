# Current sprint

**Active file:** `docs/sprints/S08-fidelity.md`
**Target:** sized grid + awake set; catch_up(K) = mean rain then settle then batched sinks
**Oracle allowlist:** `crates/sim-core/**`, `oracle/RUNLOG.md`, `.github/workflows/**`, workspace `Cargo.toml`
**Spec allowlist:** `docs/**`, `AGENTS.md`, `README.md`
