# Sprint 3.3.1 pond receiver cap

Depends on S03.3. Do not edit docs/.

S03.3 sender caps are not enough. Several donors can fill one low-H cell past every donor H, then the next tick reverses. That is the period-2 bounce in the critic (loam high H 8.37 vs valley 8.38).

Keep candidate V_ij = min(h_i w_ij, 0.5 dH, R_max=0.15).
Then for each receiver j: In = sum incoming. room = max(0, min(donor H) - H_j). If In > room, scale incoming by room/In.
H_j after <= every donor snapshot H.

Tests: d331_multi_donor_no_head_inversion, d331_drowned_lake_no_period2. Keep older names green.
