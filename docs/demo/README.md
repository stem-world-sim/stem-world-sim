# Critic demos

Download the `.html` file and open it locally (`file://`). GitHub’s source view does not run script.

- `d0.html` — one column, rain vs pore space
- `d1.html` — 3×3 map, N/E/S/W runoff, click cell → rain → one tick

These pages copy the published sprint rule. `cargo test` is the kernel.
