# kernel_catalog.csv

Typed subset for sim-core. Empty = unknown. Form defaults in S11.

S11 tests require the ten `demo=1` rows (already on main). Gold rows (form+height+envelope+root QC) live in the project working copy (~451 taxa) and can be appended later; they are not required to start S11.

No biome column. No P_max/T_wilt (kernel constants).
Rice root 6 m QC-rejected → empty root_depth_m.
