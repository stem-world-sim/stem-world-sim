# sim-core

Oracle-owned kernel. Spec chat does not add Rust here.

After Sprint 1 this directory must contain `Cargo.toml`, `src/lib.rs`, and `tests/acceptance.rs` with the function names in `docs/spec/ACCEPTANCE.md`.
